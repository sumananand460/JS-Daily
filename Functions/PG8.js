// Using arrow fucntion to find the largest number
const max = (a,b) => {
    if (a > b) {
        return `${a} > ${b}`;
    } else {
        return `${a} < ${b}` ;
    }
}

console.log(max(10,66));
    